Pressure and volume data (first two columns) come from NIST

First column is pressure in ATM
Second column is volume in L/mol
Third column is fugacities, calculated using `scripts/integrate_reference_data.py`